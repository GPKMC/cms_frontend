import TimeTabs from "./timetabs";

export default function Page() {
  // No props allowed here; just render your client component.
  return <TimeTabs />;
}

// (Optional and allowed)
// export const metadata = { title: "Time & Schedule" };
